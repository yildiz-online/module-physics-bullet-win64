sudo: required

services:
  - docker

before_install:
  - docker pull moussavdb/build-java-native

script:
  - docker run -v $PWD:/src -w /src moussavdb/build-java-native mvn -V -s settings.xml clean install sonar:sonar -Pbuild,native-sonar -Dsonar.cfamily.build-wrapper-output=target/sonar-cpp
